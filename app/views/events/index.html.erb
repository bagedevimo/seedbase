<div class="content">
  <div class="container">
    <h1>Events</h1>

    <ul>
      <% @events.each do |event| %>
        <li><%= link_to_event event.name, event %></li>
      <% end %>
    </ul>
  </div>
</div>

